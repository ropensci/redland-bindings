#!/bin/bash
# This script is called by R package installation when
# the command "R CMD INSTALL" is invoked.

echo "Running redland installation configuration"

pushd inst/build ; make all
	
echo "redland installation configuration done"
